#include<stdio.h>

int main()
{
    printf("Size of a int is %d\nSize of a float is %d\nSize of a char is %d\nSize of a double is %d",sizeof(int),sizeof(float),sizeof(char),sizeof(double));
    return 0;
}
